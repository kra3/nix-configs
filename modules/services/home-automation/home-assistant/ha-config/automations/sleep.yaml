# ── Sleep routine ──────────────────────────────────────────────
- id: sleep_tv_off
  alias: Sleep — TV off if idle
  description: >-
    Turn off TV when sleep schedule starts, but only if nobody
    is actively watching (not playing).
  triggers:
  - entity_id: schedule.sleep
    to: "on"
    trigger: state
  conditions:
  - condition: not
    conditions:
    - condition: state
      entity_id: media_player.lg_webos_tv_oled77c35la
      state: playing
  actions:
  - action: media_player.turn_off
    target:
      entity_id: media_player.lg_webos_tv_oled77c35la
  mode: single

- id: sleep_alarmo_arm_home
  alias: Sleep — Arm Alarmo (night)
  description: >-
    Arms at bedtime; Alarmo silently refuses if a sensor's open, so this
    checks the result and notifies if it failed. No actionable button on
    the notification (mobile_app's entity-based notify doesn't support
    one) — force-arming a failed attempt is a button on the dashboard
    instead (dashboard.yaml), next to the Dashboard Alert: Security
    row that lights up for this same case.
  triggers:
  - entity_id: schedule.sleep
    to: "on"
    trigger: state
  actions:
  - action: alarm_control_panel.alarm_arm_night
    target:
      entity_id: alarm_control_panel.vasudha_secure
    data:
      code: !secret alarm_code
  - delay: "00:00:05"
  - if:
    - condition: not
      conditions:
      - condition: state
        entity_id: alarm_control_panel.vasudha_secure
        state: armed_night
    then:
    - action: script.notify_all_phones
      data:
        title: Alarm not armed
        message: >-
          Couldn't arm for the night — still open:
          {{ state_attr('binary_sensor.house_exits_open_status', 'open_names') }}.
          Force-arm from the dashboard if needed.
  mode: single

# ── Wake-up ────────────────────────────────────────────────────
- id: sleep_alarmo_disarm_morning
  alias: Sleep — Disarm Alarmo (morning)
  description: >-
    Kept separate from wakeup_lighting so a fault there can't also block the disarm.
  triggers:
  - id: schedule_off
    entity_id: schedule.sleep
    to: "off"
    trigger: state
  - id: startup
    trigger: homeassistant
    event: start
  conditions:
  - condition: or
    conditions:
    - condition: trigger
      id: schedule_off
    - condition: and
      conditions:
      - condition: trigger
        id: startup
      - condition: state
        entity_id: schedule.sleep
        state: "off"
      - condition: state
        entity_id: alarm_control_panel.vasudha_secure
        state: armed_night
  actions:
  - action: alarm_control_panel.alarm_disarm
    target:
      entity_id: alarm_control_panel.vasudha_secure
    data:
      code: !secret alarm_code
  mode: single

- id: wakeup_lighting
  alias: Wake-up — Gradual lighting
  description: >-
    When sleep schedule ends, gradually brighten bedroom and hall over 10 minutes.
  triggers:
  - entity_id: schedule.sleep
    to: "off"
    trigger: state
  actions:
  - target:
      entity_id:
      - light.bedroom_wardrobe_lights
      - light.hall
    data:
      brightness_pct: 5
      transition: 1
    action: light.turn_on
  - delay: "00:02:00"
  - target:
      entity_id:
      - light.bedroom_wardrobe_lights
      - light.hall
    data:
      brightness_pct: 15
      transition: 60
    action: light.turn_on
  - delay: "00:03:00"
  - target:
      entity_id:
      - light.bedroom_wardrobe_lights
      - light.hall
    data:
      brightness_pct: 40
      transition: 60
    action: light.turn_on
  - delay: "00:05:00"
  - target:
      entity_id:
      - light.bedroom_wardrobe_lights
      - light.hall
    data:
      brightness_pct: 60
      transition: 60
    action: light.turn_on
  mode: restart
