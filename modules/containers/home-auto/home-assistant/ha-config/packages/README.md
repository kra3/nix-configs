# Declarative automations go here. Loaded via !include_dir_named packages in configuration.yaml.
# Format: each file contains a list under `automation:` key, e.g.:
#
# automation:
#   - alias: "My automation"
#     trigger: ...
#     action: ...
