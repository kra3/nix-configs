[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "beets-normalize-names"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["beetsplug*"]
namespaces = true
